export { APP_VERSION } from './utils/appVersion';

/** Store URLs — update before publishing to stores. */
export const STORE_URLS = {
  ios: 'https://apps.apple.com/app/kingdomkeeper/id0000000000',
  android: 'https://play.google.com/store/apps/details?id=com.kingdomkeeper.mobile',
} as const;
